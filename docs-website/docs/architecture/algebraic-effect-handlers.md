# Algebraic Effect Handlers

*Coming soon.*

How `loop`, `tryCatch`, `earlyReturn`, and `recur` are implemented as algebraic effect handlers in the Rust runtime — and why this model enables composable control flow without callbacks or exceptions.
