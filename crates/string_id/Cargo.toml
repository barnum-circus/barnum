[package]
name = "string_id"
version = "0.1.0"
edition = "2024"

[dependencies]
schemars = "0.8"
serde = { version = "1.0", features = ["derive"] }

# These dependencies are used in macro-generated code, not directly
[package.metadata.cargo-udeps.ignore]
normal = ["schemars", "serde"]
