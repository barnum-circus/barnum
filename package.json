{
  "scripts": {
    "test": "bash scripts/quiet-test.sh && cargo test --workspace",
    "typecheck": "bash -c 'set -e; for pkg in libs/barnum demos/*/; do [ -f \"$pkg/tsconfig.json\" ] && echo \"Typechecking $pkg...\" && (cd \"$pkg\" && pnpm run typecheck); done'"
  }
}
